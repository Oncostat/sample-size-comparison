---
title: R vs East & nQuery 
subtitle: Utilisation de R comme alternative aux logiciels propriétaires East et nQuery pour le calcul de Sample Size.
author:
  - name: AROCENA Thomas 
    orcid: 0009-0002-7014-220X
abstract: |
  {{< lipsum 1 >}}
papersize: a4
format: 
  typst:
    mathfont: Noto Sans Math
keywords:
  - Quarto
  - Typst
  - format
date: today
date-format: full
logo:
  width: 1in
  padding: 0.375in
  location: left-top
brand: brand\_brand.yml
execute:
  echo: false
margin: 
  x: 1.25in
  y: 1.25in
lang: "fr"
---

```{r}
#| label: package_setup
#| echo: false
#| message: false
#| warning: false
library(gt)
library(rpact)
library(tidyverse)
library(rashnu)
```

## Comparaison I : Sample size pour tester le hazard ratio dans un groupe à 2 bras.

### Méthode de Schoenfeld

Le calcul de la taille d'échantillon de Schoenfeld, comme les autres méthodes de log rank se fait sous les hypothèses de loi exponentielles et de proportional hazard qui ne sont pas complètement satisfaisantes dans certain cas mais complètement ancré dans l'analyse de survie en générale.

Prenons $\alpha$ l'erreur de type I et $\beta$ l'erreur de type II. Alors, en notant $e$ le nombre d'evènement nécessaire, la formule (simple) de Schoenfeld donne :

$$
e = 2\times\frac{2(z_{1-\alpha/2} + z_{1 - \beta})^2}{\log(\text{HR})}
$${#eq-schoenfeld}

Où $z_x$ est le [z-score](https://en.wikipedia.org/wiki/Standard_score) de $x$, égale au quantile $(1-x)$ de la distribution normale standard.

### Méthode de Lakatos

blabla 

### Paramètres

On va comparer les resultats de Rpact et de East sur l'ensemble de test suivant :

$\alpha \in \{0.01, 0.05, 0.1, 0.20, 0.49\}$

$\text{power} \in \{0.51, 0.8, 0.9, 0.99\}$

$\text{HR} \in \{0.1, 0.5, 0.7, 0.9, 0.99\}$

$\text{Survie à 3 ans} \in \{0.1, 0.3, 0.6, 0.9\}$

```{r}
#| label: param_table_setup
param_list = list(
  alpha = c(0.01, 0.05, 0.1, 0.20, 0.49),
  power = c(0.51, 0.8, 0.9, 0.99),
  hr = c(0.1, 0.5, 0.7, 0.9, 0.99),
  surv_t = c(0.1, 0.3, 0.6, 0.9)
)

# Get all combinaisons of params
param_table <- 
  param_list |> 
  expand.grid() |> 
  as_tibble()
```

::: {.block fill="luma(200)" inset="8pt" radius="4pt"}
This is a block with gray background and slightly rounded corners.
:::

### Rpact

On utilise la fonction `getSampleSizeSurvival` pour estimer 
le nombre d'évènements et de patients nécessaires.


```{r}
#| label: rpact_res
#| cache: true
source("R/rpact_wrapper.R")

rpact_res <- 
  param_table |> 
  mutate(
    nested_res = pmap(param_table, rpact_wrapper)
    ) |> 
  unnest(nested_res) |> 
  rename(e_rpact = e, n_rpact = n)

#write_csv(p_res, "results/rpact_grid.csv")
#write_csv2(p_res, "results/rpact_grid.csv")
```

### East

```{r}
#| label: east_res
# As East has problems running the 400 inputs consecutively
# I ran 4*100 by spliting by power value 
east_res_dirty <- bind_rows(
  read_csv("results/east_pwr051.csv"),
  read_csv("results/east_pwr08.csv"),
  read_csv("results/east_pwr09.csv"),
  read_csv("results/east_pwr099.csv")
)

# Keep only needed input/output
# and add the survival rate at 3 years that is
# missing in East output
east_res <- east_res_dirty |> 
  select(
    alpha = "Specified α",
    power = "Power",
    hr = "Hazard Ratio (Alt.)",
    e_east = "Maximum Events",
    n_east = "Sample Size"
  ) |> 
    arrange(alpha, power, hr, n_east) |> 
    mutate(surv_t = rep(c(0.1, 0.3, 0.6, 0.9), n()/4)) |> 
    mutate(power = signif(power, 1))
```

### nQuery

```{r}
#| label: nquery_res

nquery_res_dirty <- read.csv2("results/nquery_grid.csv", dec = ",")

nquery_res_less_dirty <- data.frame(t(nquery_res_dirty[-1]))
colnames(nquery_res_less_dirty) <- nquery_res_dirty[,1] 

nquery_res <- 
  nquery_res_less_dirty |> 
  tibble() |> 
  select(
    alpha = "Test Significance Level, ??",
    power = "Power (%)?",
    hr = "Hazard Ratio, h=??/???",
    lambda1 = "Group 1 Exponential Parameter, ???",
    e_nquery = "Total Number of Events Required, E?",
    n_nquery = "Sample Size per Group, n?"
  ) |> 
    mutate(
      across(
        c(alpha, power, hr, lambda1),
        \(x){parse_number(x, locale = locale(decimal_mark = ","))}
      ),
      lambda1 = signif(lambda1, 1),
      surv_t = case_when(
        lambda1 == 0.80 ~ 0.1,
        lambda1 == 0.40 ~ 0.3,
        lambda1 == 0.20 ~ 0.6,
        lambda1 == 0.04 ~ 0.9,
        .default = NA_real_
      ),
      across(c(e_nquery, n_nquery), as.numeric),
      power = power/100,
      n_nquery = 2*n_nquery # introduce an error cause all n will be even.
    ) |> 
    select(- lambda1)
```


### Rashnu

```{r}
#| label: rashnu_res
#| cache: true

source("R/rashnu_wrapper.R")

rashnu_res <- 
  param_table |> 
  mutate(
    nested_res = pmap(param_table, rashnu_wrapper)
    ) |> 
  unnest(nested_res) |> 
  rename(e_rashnu = e, n_rashnu = n)
```

### Inter-method comparison

```{r}
#| label: combined_res

source("R/evaluate_relevancy.R")

combined_res <- 
  reduce(
    list(rpact_res, east_res, nquery_res, rashnu_res),
    \(x, y){inner_join(x, y, by = join_by(alpha, power, hr, surv_t))}
  ) |> 
  mutate(relevancy = evaluate_relevancy(alpha, power, hr)) |> 
   mutate(
    relevancy = fct_relevel(
      relevancy,
      c("low", "medium", "high")
    )
   )
```


```{r}
#| label: compute_n_ratio

n_ratio_by_method <- 
  combined_res |> 
  mutate(
    rpact = n_rpact/n_east,
    nquery = n_nquery/n_east,
    rashnu = n_rashnu/n_east,
    relevancy = evaluate_relevancy(alpha, power, hr)
  ) |> 
  mutate(
    relevancy = fct_relevel(
      relevancy,
      c("low", "medium", "high")
    )
  ) |> 
  select(c(alpha, power, hr, surv_t, relevancy, n_east, rpact, nquery, rashnu)) |> 
  pivot_longer(c(rpact, nquery, rashnu), names_to = "method", values_to = "n_ratio")
```

```{r}
#| label: n_nquery_analysis
n_nquery_analysis <- 
  n_ratio_by_method |> 
  filter(method == "nquery", relevancy == "high") |> 
  mutate(
    hr = as.character(hr),
    surv_t = as.character(surv_t)
  )

ggplot(n_nquery_analysis) +
  aes(x = n_east, y = n_ratio, color = hr, shape = surv_t) +
  geom_point()
```

### Lakatos and Lan (1992)

```{r}
#| label: ll_param_table_setup

ll_param_list = list(
  alpha = 0.05,
  power = 0.9,
  hr = c(0.667, 0.5, 0.25),
  surv_t = c(0.8, 0.2),
  event_time = 10,
  accrual_time = c(1, 5, 9),
  follow_up_time = c(9, 5, 1)
)

# Get all combinaisons of params with 10 years study duration
ll_param_table <- 
  ll_param_list |> 
  expand.grid() |> 
  as_tibble() |> 
  filter(accrual_time + follow_up_time == 10)
```


#### rpact
```{r}
#| label: rpact_res_ll
source("R/rpact_wrapper.R")

rpact_res_ll <- 
  ll_param_table |> 
  mutate(
    nested_res = pmap(ll_param_table, rpact_wrapper)
    ) |> 
  unnest(nested_res) |> 
  rename(e_rpact = e, n_rpact = n)

rpact_res_ll_presentation <-
  rpact_res_ll |> 
  select(surv_t, hr, accrual_time, n_rpact) |> 
  arrange(desc(surv_t), desc(hr))
```
#### East

```{r}
#| label: east_res_ll

east_res_dirty_ll <- read_csv("results/east_lakatos_n_lan.csv")

# Keep only needed input/output
# and add the survival rate at 3 years that is
# missing in East output
east_res_ll <- 
  east_res_dirty_ll |> 
  select(
    alpha = "Specified α",
    power = "Power",
    hr = "Hazard Ratio (Alt.)",
    accrual_time = "Comm. Accr. (Dur.)",
    e_east = "Maximum Events",
    n_east = "Sample Size"
  ) |> 
    arrange(hr, accrual_time, n_east) |> 
    mutate(surv_t = rep(c(0.2, 0.8), n()/2)) |> 
    mutate(power = signif(power, 1))

east_res_ll_presentation <-
  east_res_ll |> 
  select(surv_t, hr, accrual_time, n_east) |> 
  arrange(desc(surv_t), desc(hr)) 
```

#### nQuery 

```{r}
#| label: nquery_res_ll

nquery_res_dirty_ll <- read.csv2("results/nquery_lakatos_n_lan.csv", dec = ",")

nquery_res_less_dirty_ll <- data.frame(t(nquery_res_dirty_ll[-1]))
colnames(nquery_res_less_dirty_ll) <- nquery_res_dirty_ll[,1] 

nquery_res_ll <- 
  nquery_res_less_dirty_ll |> 
  tibble() |> 
  select(
    alpha = "Test Significance Level, ??",
    power = "Power (%)?",
    hr = "Hazard Ratio, h=??/???",
    lambda1 = "Group 1 Exponential Parameter, ???",
    accrual_time = "Length of Accrual Period, a?",
    e_nquery = "Total Number of Events Required, E?",
    n_nquery = "Sample Size per Group, n?"
  ) |> 
    mutate(
      across(
        c(alpha, power, hr, lambda1),
        \(x){parse_number(x, locale = locale(decimal_mark = ","))}
      ),
      lambda1 = signif(lambda1, 1),
      hr = signif(hr, 3),
      accrual_time = as.numeric(accrual_time),
      surv_t = case_when(
        lambda1 == 0.2 ~ 0.2,
        lambda1 == 0.02 ~ 0.8,
        .default = NA_real_
      ),
      across(c(e_nquery, n_nquery), as.numeric),
      power = power/100,
      n_nquery = 2*n_nquery # introduce an error cause all n will be even.
    ) |> 
    select(-lambda1)


nquery_res_ll_presentation <-
  nquery_res_ll |> 
  select(surv_t, hr, accrual_time, n_nquery) |> 
  arrange(desc(surv_t), desc(hr))
```

#### Rashnu

```{r}
#| label: rashnu_res_ll
source("R/rashnu_wrapper.R")

rashnu_res_ll <- 
  ll_param_table |> 
  mutate(
    nested_res = pmap(ll_param_table, rashnu_wrapper)
    ) |> 
  unnest(nested_res) |> 
  rename(e_rashnu = e, n_rashnu = n)

rashnu_res_ll_presentation <-
  rashnu_res_ll |> 
  select(surv_t, hr, accrual_time, n_rashnu) |> 
  arrange(desc(surv_t), desc(hr))
```

#### Comparision

```{r}
#| label: combined_res_ll
combined_res_ll <- 
  reduce(
    list(
      rpact_res_ll_presentation,
      east_res_ll_presentation,
      nquery_res_ll_presentation,
      rashnu_res_ll_presentation,
      design_table),
    \(x, y){inner_join(x, y, by = join_by(hr, surv_t, accrual_time))}
  )

combined_res_ll  |> gt(rowname_col = c("surv_t", "hr", "accrual_time"))
```

```{r}

design_table = tribble(
  ~surv_t, ~hr, ~accrual_time, ~n_L, ~n_F, ~n_RGS, ~power_L, ~power_F, ~power_RGS,
  0.8, 0.667, 1, 1617, 1628, 1640, 90.2, 90.4, 90.6,
  0.8, 0.667, 5, 2017, 2024, 2046, 90.6, 90.7, 91.0,
  0.8, 0.667, 9, 2724, 2709, 2764, 90.3, 90.1, 90.7,

  0.8, 0.50, 1, 638, 649, 664, 90.1, 90.6, 91.2,
  0.8, 0.50, 5, 798, 807, 831, 90.7, 91.0, 91.8,
  0.8, 0.50, 9, 1079, 1081, 1124, 90.1, 90.2, 91.2,

  0.8, 0.25, 1, 230, 241, 269, 91.9, 93.0, 95.3,
  0.8, 0.25, 5, 289, 299, 338, 92.2, 93.0, 95.5,
  0.8, 0.25, 9, 392, 401, 459, 91.6, 92.2, 95.1,

  0.2, 0.667, 1, 360, 370, 363, 89.6, 90.4, 89.8,
  0.2, 0.667, 5, 414, 419, 418, 90.5, 90.8, 90.8,
  0.2, 0.667, 9, 528, 509, 534, 89.8, 88.7, 90.1,

  0.2, 0.50, 1, 134, 144, 138, 89.7, 91.7, 90.5,
  0.2, 0.50, 5, 156, 164, 161, 89.9, 91.3, 90.8,
  0.2, 0.50, 9, 200, 200, 207, 89.7, 89.7, 90.7,

  0.2, 0.25, 1, 43, 53, 48, 90.2, 95.1, 93.0,
  0.2, 0.25, 5, 51, 61, 58, 90.6, 94.8, 93.8,
  0.2, 0.25, 9, 66, 74, 76, 90.2, 93.1, 93.7
)
```

## Comparison II : Group sequential design

### Rpact

see [rpact doc](https://www.rpact.org/vignettes/planning/rpact_survival_examples/#sample-size-calculation-for-trials-with-interim-analyses)

```{r}
design <- getDesignGroupSequential(
    sided = 2, 
    alpha = 0.05, 
    beta = 0.2,
    informationRates = c(0.25, 0.5, 0.75, 1),
    typeOfDesign = "asOF",
    typeBetaSpending = "bsOF",
    gammaB = 2.5,
    bindingFutility = FALSE,
)
```

```{r}
rpact_gsdesign <- 
  getDesignGroupSequential(
    sided = 2,
    alpha = 0.05,
    beta = 0.1,
    kMax = 4, 
    informationRates = c(0.25, 0.5, 0.75, 1), # also the values by default.
    typeOfDesign = "asOF",
    typeBetaSpending = "bsOF", 
    bindingFutility = FALSE
  )
```

```{r}
sample_size_info_gs = getSampleSizeSurvival(
  design = rpact_gsdesign,
  hazardRatio = 0.6,
  pi2 = 0.2,
  eventTime = 3,
  typeOfComputation = "Schoenfeld",
  followUpTime = 3,
  accrualTime = c(0, 3),
  allocationRatioPlanned = 1,
  dropoutRate1 = 0,
  dropoutRate2 = 0
)
```

### East

J'ai observé une variabilité entre 2 computes sur East de même paramètres, à vérifier.
Pas de variabilité sur rpact.

```{r}
filelist <- 
  list.files(path = "results/East_gsdesign", full.names = TRUE)
```

```{r}
east_res_garbage <-
  read_csv(filelist)
#  select(where(is.numeric))  |> 
# select(!where(anyNA))  |> 
# select(where(~any(. > 0)))  |> 
# select(where(~n_distinct(.) > 1)) |> 
#  mutate(across(where(is.numeric), ~ signif(., 4)))

# east_res_garbage <- east_res_garbage[!duplicated(as.list(east_res_garbage))]

east_res_less_garbage <-
  east_res_garbage |> 
  select(
    alpha = Alpha__AAA,
    power = Power__AAA,
    hr = HazardRatio__AAAMA,
    surv_t = CumPercSurvCtrl__AAAMAA_AE, #In percentages
    e_east = TotSmplSiz__AB,
    n_east = A1x0__ABBE_BA
  )
```